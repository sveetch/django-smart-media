
class FieldFinder:
    """
    Find all model field from definitions (Blog, Blog.image, etc..) and gather under a
    model name index dict.
    """
    pass


class DataExplorer:
    """
    Efficiently scan database for given models fields to find all stored and used media
    files, so every files found from media storage (default backend) that are not used
    from data.
    """
    pass
